# LightVSDecay Unity 编码规范

本文件用于约束 Codex 在 `LightVSDecay` 项目中的分析、修改、重构与排错行为。目标是保证修改可控、风格统一、适配现有项目结构，并尽量降低引入回归问题的概率。

## 1. 项目定位

- 项目类型：Unity 2D 休闲塔防 / Roguelike / 光与朽
- 当前核心平台特征：Unity、URP、UGUI、TextMeshPro、对象池、微信小游戏适配
- 当前协作目标：
  - 读取并理解现有工程
  - 协助定位和修复 Bug
  - 在不破坏既有结构的前提下完善功能

## 2. 目录与职责

优先遵守当前项目的目录边界，不随意跨层堆逻辑。

- `Assets/Scripts/Core`
  - 放基础设施、全局事件、单例、日志、通用工具、对象池
- `Assets/Scripts/Logic`
  - 放核心玩法逻辑、战斗、波次、敌人、Boss、玩家技能
- `Assets/Scripts/Data`
  - 放 ScriptableObject、运行时存档数据、配置数据
- `Assets/Scripts/UI`
  - 放界面表现、面板、HUD、浮字、科技树、装备界面
- `Assets/Scripts/Audio`
  - 放音频配置与音频控制逻辑
- `Assets/Scripts/VFX`
  - 放表现层特效控制
- `Assets/Editor`
  - 只放编辑器工具，不放运行时代码

禁止把 UI 逻辑直接写进 Data 层；禁止把配置常量散落到多个玩法脚本中。

## 3. 命名空间与命名规范

- 严格沿用现有命名空间前缀：`LightVsDecay.*`
- 新增脚本必须放入对应命名空间，不允许无命名空间裸类
- 类名、方法名、属性名：`PascalCase`
- 私有字段：`camelCase`
- 常量：`UPPER_SNAKE_CASE`
- Inspector 暴露字段优先使用：
  - `[SerializeField] private`
- 非必要不要使用 `public` 字段直接暴露数据
- 布尔变量尽量使用可读前缀：
  - `is`
  - `has`
  - `can`
  - `should`

## 4. 现有架构约束

### 4.1 事件通信优先

项目已有 `Assets/Scripts/Core/GameEvents.cs` 全局事件总线。跨模块通信优先考虑事件，而不是直接相互查找和硬引用。

优先顺序：

1. 同组件内部直接调用
2. 同模块明确依赖时通过字段引用
3. 跨模块通信优先 `GameEvents`

以下情况优先走事件：

- 游戏状态变化
- 波次开始/结束
- 玩家数值变化
- UI 刷新通知
- Boss 战状态切换
- 技能应用结果广播

不要为了一个小需求随意继续膨胀 `GameEvents`；新增事件前先确认是否真的是跨模块广播需求。

### 4.2 日志统一

项目已有 `Assets/Scripts/Core/GameLogger.cs`。

- 默认使用 `GameLogger.Log/LogWarning/LogError`
- 不直接散落 `Debug.Log`
- 高频逻辑中避免反复打印日志
- 调试日志应受开关控制，如 `showDebugInfo`
- 修复完成后，临时排查日志要及时删除或降级

### 4.3 单例与管理器

项目已有 `Singleton` / `PersistentSingleton` / `AutoPersistentSingleton`。

- 仅在确实属于全局唯一系统时使用单例
- 不要新增“伪 Manager”制造更多全局状态
- 修改单例生命周期时，必须检查：
  - 场景切换后是否重复创建
  - 事件订阅是否泄漏
  - `OnDestroy` / 反注册是否完整

## 5. Unity 编码要求

### 5.1 生命周期函数

- 初始化引用优先放 `Awake` / `Start`
- 注册事件与取消注册必须成对出现
- 若类继承项目内单例基类，优先遵守其生命周期钩子，如 `OnSingletonAwake`

### 5.2 Inspector 字段

- 所有需要策划或美术在 Inspector 调整的字段，使用 `[SerializeField] private`
- 能通过 Tooltip 或 Header 提升可配置性时，应适度补充
- 不在运行时频繁依赖 `FindObjectOfType` / `GameObject.Find`
- 若确需兜底查找，只允许在初始化路径中执行，不允许在高频路径中执行

### 5.3 Null 与 Unity Object

由于 Unity 的伪 null 机制：

- 对 `UnityEngine.Object` 派生对象，优先使用 `obj == null`
- 不滥用 `?.` 处理 Unity 对象
- 外部引用、Prefab 组件、场景对象必须做好判空保护

### 5.4 时间系统

项目中存在暂停、Boss 战、UI 弹窗等逻辑，所有时间相关代码必须明确是否受 `Time.timeScale` 影响。

- 战斗推进通常用 `Time.deltaTime`
- 暂停时仍需工作的 UI / 动画 / 倒计时，使用 `Time.unscaledDeltaTime`
- 新增协程和延迟逻辑前，先确认暂停时是否应继续运行

## 6. 性能与内存约束

### 6.1 对象池强制要求

项目已有 `Assets/Scripts/Core/Pool`。

以下对象默认视为高频对象，禁止直接频繁 `Instantiate/Destroy`：

- 敌人
- 子弹 / 激光派生物
- 掉落物
- 浮字
- VFX
- 可重复刷新的特效实体

新增高频对象时优先复用现有对象池模式。

### 6.2 Update/FixedUpdate 约束

在 `Update`、激光 tick、波次推进、敌人 AI 等高频逻辑中：

- 禁止频繁 `GetComponent`
- 禁止频繁 `FindObjectOfType`
- 禁止频繁 `GameObject.Find`
- 禁止不必要的 GC 分配
- 能缓存的引用必须缓存

### 6.3 集合与临时对象

- 高频逻辑优先复用 `List`、`HashSet`、数组缓冲区
- 避免在每帧或每 tick 中创建临时集合
- 协程中避免反复 `new WaitForSeconds`

## 7. 针对当前项目的专项规则

### 7.1 激光系统

`LaserController` 已经拆分为多个子模块，后续修改遵循：

- 优先在对应子模块中改动，而不是继续堆大 `LaserController`
- 伤害、暴击、击退、穿透、冻结、音频逻辑尽量各归其位
- 若新增激光效果，先判断属于：
  - 数值计算
  - 命中处理
  - 表现控制
  - 音频/VFX 同步

不要把所有新逻辑继续塞回主控制器。

### 7.2 GameManager 与场景切换

项目已有：

- `SafeSceneLoader`
- `GameSessionConfig`
- `GameEvents.ClearAllEvents()`

涉及场景切换、重开、返回主菜单时，必须检查：

- 事件是否被重复注册
- 会话配置是否被错误重置
- `Time.timeScale` 是否恢复
- 持久单例是否残留脏状态

### 7.3 ScriptableObject 配置优先

以下内容优先放配置，不要硬编码进玩法脚本：

- 波次参数
- 章节配置
- Boss 配置
- 敌人数值
- 技能数值
- 奖励配置
- 科技树配置

### 7.4 UI 与逻辑解耦

- UI 层负责显示与交互，不负责核心数值计算
- UI 刷新尽量由数据变化驱动
- 不要在 UI 脚本中硬写战斗核心规则

### 7.5 微信小游戏 / WebGL 兼容

项目依赖中含微信小游戏相关包，修改时注意：

- 场景加载和异步流程优先复用已有安全封装
- 避免引入依赖线程、反射重、平台兼容性差的写法
- 涉及文件系统、路径、平台 API 的改动要先确认是否影响小游戏构建

## 8. 修改策略

### 8.1 Bug 修复优先级

修 Bug 时优先顺序：

1. 先定位根因
2. 优先小范围修复
3. 保持现有行为稳定
4. 补充必要防御
5. 仅在必要时做局部重构

不要为了修一个点，顺手重写整块系统。

### 8.2 重构边界

允许：

- 提取私有方法
- 提取小型辅助类
- 清理重复逻辑
- 补判空、补缓存、补注释

谨慎：

- 修改公开接口
- 修改 ScriptableObject 字段名
- 修改场景绑定字段
- 修改事件签名
- 修改存档结构

这些改动一旦发生，必须同时评估对 Inspector、Prefab、场景引用和旧数据兼容性的影响。

### 8.3 注释原则

- 只在复杂逻辑、易误解逻辑、Unity 特殊坑位上加注释
- 注释应解释“为什么”，不要机械解释“做了什么”
- 不要输出大段无价值注释

## 9. Codex 工作方式

Codex 在本项目中协作时遵守以下行为：

- 先读上下文，再动代码
- 先查看相关脚本、配置和调用链，再给结论
- 一次修改尽量聚焦一个问题
- 修改前说明将要改哪些文件
- 修改后说明影响范围、验证建议、潜在风险
- 除非用户明确要求，不主动改动：
  - `Library/`
  - `Temp/`
  - `Logs/`
  - `obj/`
  - 自动生成文件

## 10. 排查问题时的默认检查清单

遇到 Bug 时，优先检查：

- Inspector 引用是否丢失
- 事件是否重复订阅或未注销
- 场景切换后单例是否脏状态残留
- `Time.timeScale` 是否导致逻辑停摆
- 高级功能是否被配置覆盖
- ScriptableObject 配置是否为空或越界
- 对象池对象回收后状态是否重置完整
- 是否存在初始化顺序问题
- 是否有平台兼容分支遗漏

## 11. 默认不做的事情

- 不随意重命名场景绑定字段
- 不随意移动脚本目录导致 meta 变化
- 不随意修改大型核心系统架构
- 不把临时调试代码长期留在主逻辑
- 不把测试脚本混入正式运行时代码目录

## 12. 推荐的新增内容风格

新增脚本时优先保持以下风格：

- 文件头可有简短用途说明，但不要冗长
- 字段按“配置引用 / 运行时缓存 / 状态 / 常量 / 属性 / 生命周期 / 核心逻辑”分段
- 复杂组件优先拆为小模块，而不是做超大 MonoBehaviour
- 能复用现有系统就复用：
  - `GameEvents`
  - `GameLogger`
  - `ObjectPool`
  - 现有 Manager / Config / UI 流程

## 13. 本项目协作结论

对于 `LightVSDecay`，Codex 的默认目标是：

- 以最小风险帮助你修复现有 Bug
- 在遵守当前工程结构的前提下补全功能
- 优先保护战斗主流程、场景流程、配置数据、UI 绑定和对象池稳定性

如果需求存在不确定性，先询问再做高风险改动。
